- publisher:
    name: send-email-notification
    publishers:
      - email:
         recipients: ci@localhost

- builder:
    name: copy-artifact
    builders:
      - copyartifact:
          project: "{project_name}"
          target: "artifacts/"
